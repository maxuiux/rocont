/******/ (() => { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./src/js/libs/swiper/items-slider.js":
/*!********************************************!*\
  !*** ./src/js/libs/swiper/items-slider.js ***!
  \********************************************/
/***/ (() => {

var swiper = new Swiper('[data-component="items-slider:slider-stories"]', {
  slidesPerView: 'auto',
  effect: 'slide',
  spaceBetween: 8,
  centeredSlides: false,
  grabCursor: true,
  breakpoints: {
    320: {
      slidesPerView: 'auto',
      effect: 'none',
      spaceBetween: 8
    },
    484: {
      slidesPerView: 'auto',
      effect: 'slide'
    }
  },
  navigation: {
    nextEl: '[data-element="items-slider:next"]',
    prevEl: '[data-element="items-slider:prev"]'
  }
});

/***/ }),

/***/ "./src/js/widgets/current-year.js":
/*!****************************************!*\
  !*** ./src/js/widgets/current-year.js ***!
  \****************************************/
/***/ (() => {

document.addEventListener('DOMContentLoaded', function () {
  var yearElements = document.querySelectorAll('[data-current-year="true"]');

  if (yearElements.length > 0) {
    var currentYear = new Date().getFullYear();
    yearElements.forEach(function (element) {
      element.textContent = currentYear;
    });
  }
});

/***/ }),

/***/ "./src/js/widgets/ios-video.js":
/*!*************************************!*\
  !*** ./src/js/widgets/ios-video.js ***!
  \*************************************/
/***/ (() => {

document.addEventListener('DOMContentLoaded', function () {
  var iosVideos = document.querySelectorAll('[data-ios-video="ios-video"]');
  iosVideos.forEach(function (video) {
    // Отключаем взаимодействие пользователя
    video.style.pointerEvents = 'none'; // Принудительное воспроизведение через user gesture симуляцию

    var tryPlay = function tryPlay() {
      video.play()["catch"](function () {
        // Если не получилось, пробуем снова после взаимодействия
        var unlock = function unlock() {
          video.play()["finally"](function () {
            document.body.removeEventListener('touchstart', unlock);
            document.body.removeEventListener('click', unlock);
          });
        };

        document.body.addEventListener('touchstart', unlock, {
          once: true
        });
        document.body.addEventListener('click', unlock, {
          once: true
        });
      });
    }; // Проверяем готовность видео


    if (video.readyState >= 3) {
      tryPlay();
    } else {
      video.addEventListener('canplay', tryPlay, {
        once: true
      });
    }
  });
});

/***/ }),

/***/ "./src/js/widgets/mask-phone-number.js":
/*!*********************************************!*\
  !*** ./src/js/widgets/mask-phone-number.js ***!
  \*********************************************/
/***/ (() => {

document.addEventListener('DOMContentLoaded', function () {
  var inputs = document.querySelectorAll('[data-phone="number"]');
  var template = '+7 (___) ___-__-__';
  var maskPositions = [4, 5, 6, 9, 10, 11, 13, 14, 16, 17];
  inputs.forEach(function (input) {
    input.addEventListener('focus', handleFocus);
    input.addEventListener('input', handleInput);
    input.addEventListener('blur', handleBlur);
    input.addEventListener('keydown', handleKeyDown);
    input.addEventListener('paste', handlePaste);

    function handleFocus() {
      if (!input.value) {
        input.value = template;
        setCursorToFirstPlaceholder();
      } else {
        restoreCursor();
      }
    }

    function handleBlur() {
      if (input.value === template) {
        input.value = '';
      }
    }

    function handleInput() {
      var digits = extractDigits(input.value);
      input.value = applyMask(digits);
      restoreCursor();
    }

    function handlePaste(e) {
      e.preventDefault();
      var pasted = (e.clipboardData || window.clipboardData).getData('text');
      var digits = extractDigits(pasted);
      input.value = applyMask(digits);
      restoreCursor();
    }

    function handleKeyDown(e) {
      var selStart = input.selectionStart;
      var selEnd = input.selectionEnd;
      var fullSelect = selStart === 0 && selEnd === input.value.length;
      var isBackspace = e.key === 'Backspace';
      var isDelete = e.key === 'Delete';

      if ((isBackspace || isDelete) && fullSelect) {
        e.preventDefault();
        input.value = template;
        setCursorToFirstPlaceholder();
        return;
      }

      if (isBackspace && selStart === selEnd) {
        e.preventDefault();
        var digits = extractDigits(input.value);
        var maskIndex = findPrevDigitPosition(selStart);

        if (maskIndex !== -1 && maskIndex < digits.length) {
          digits.splice(maskIndex, 1);
          input.value = applyMask(digits);
          setCursorPosition(maskPositions[maskIndex]);
        }
      }

      if (isDelete && selStart === selEnd) {
        e.preventDefault();

        var _digits = extractDigits(input.value);

        var _maskIndex = findNextDigitPosition(selStart);

        if (_maskIndex !== -1 && _maskIndex < _digits.length) {
          _digits.splice(_maskIndex, 1);

          input.value = applyMask(_digits);
          setCursorPosition(maskPositions[_maskIndex]);
        }
      }
    } // ⬇️ 🔧 Вот здесь учтены и +7 и 8


    function extractDigits(str) {
      var raw = str.replace(/\D/g, '');
      var cleared = raw.replace(/^(\+?7|8)/, ''); // удаляет и +7 и 8

      return cleared.split('').slice(0, 10);
    }

    function applyMask(digits) {
      var i = 0;
      return template.replace(/_/g, function () {
        return i < digits.length ? digits[i++] : '_';
      });
    }

    function findPrevDigitPosition(pos) {
      for (var i = maskPositions.length - 1; i >= 0; i--) {
        if (maskPositions[i] < pos) return i;
      }

      return -1;
    }

    function findNextDigitPosition(pos) {
      for (var i = 0; i < maskPositions.length; i++) {
        if (maskPositions[i] >= pos) return i;
      }

      return -1;
    }

    function setCursorToFirstPlaceholder() {
      var pos = input.value.indexOf('_');
      setCursorPosition(pos === -1 ? input.value.length : pos);
    }

    function restoreCursor() {
      var pos = input.value.indexOf('_');
      setCursorPosition(pos === -1 ? input.value.length : pos);
    }

    function setCursorPosition(pos) {
      requestAnimationFrame(function () {
        input.setSelectionRange(pos, pos);
      });
    }
  });
});

/***/ }),

/***/ "./src/js/widgets/mobile-menu.js":
/*!***************************************!*\
  !*** ./src/js/widgets/mobile-menu.js ***!
  \***************************************/
/***/ (() => {

document.addEventListener('DOMContentLoaded', function () {
  var hamburgerBtn = document.querySelector('[data-button-hamburger="button-hamburger"]');
  var sidebar = document.querySelector('[data-sidebar-active]');
  var body = document.body;
  if (!hamburgerBtn || !sidebar) return;

  var toggleSidebar = function toggleSidebar() {
    var isActive = sidebar.getAttribute('data-sidebar-active') === 'true';

    if (isActive) {
      sidebar.setAttribute('data-sidebar-active', 'false');
      body.classList.remove('overflow-sidebar');
    } else {
      sidebar.setAttribute('data-sidebar-active', 'true');
      body.classList.add('overflow-sidebar');
    }
  };

  var closeSidebar = function closeSidebar() {
    sidebar.setAttribute('data-sidebar-active', 'false');
    body.classList.remove('overflow-sidebar');
  };

  hamburgerBtn.addEventListener('click', function (e) {
    e.stopPropagation();
    toggleSidebar();
  });
  document.addEventListener('click', function (e) {
    var isClickInsideSidebar = sidebar.contains(e.target);
    var isClickOnHamburger = hamburgerBtn.contains(e.target);
    var isActive = sidebar.getAttribute('data-sidebar-active') === 'true';

    if (isActive && !isClickInsideSidebar && !isClickOnHamburger) {
      closeSidebar();
    }
  });
});

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
/*!*************************!*\
  !*** ./src/js/index.js ***!
  \*************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _widgets_mobile_menu__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./widgets/mobile-menu */ "./src/js/widgets/mobile-menu.js");
/* harmony import */ var _widgets_mobile_menu__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_widgets_mobile_menu__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _widgets_ios_video__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./widgets/ios-video */ "./src/js/widgets/ios-video.js");
/* harmony import */ var _widgets_ios_video__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_widgets_ios_video__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _widgets_mask_phone_number__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./widgets/mask-phone-number */ "./src/js/widgets/mask-phone-number.js");
/* harmony import */ var _widgets_mask_phone_number__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_widgets_mask_phone_number__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var _widgets_current_year__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./widgets/current-year */ "./src/js/widgets/current-year.js");
/* harmony import */ var _widgets_current_year__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_widgets_current_year__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _libs_swiper_items_slider__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./libs/swiper/items-slider */ "./src/js/libs/swiper/items-slider.js");
/* harmony import */ var _libs_swiper_items_slider__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(_libs_swiper_items_slider__WEBPACK_IMPORTED_MODULE_4__);
// import './widgets/header'





})();

/******/ })()
;
//# sourceMappingURL=index.js.map